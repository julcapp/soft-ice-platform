'use strict';

const { CatalogRepository } = require('./CatalogRepository');
const { CatalogService, CATEGORIES } = require('./CatalogService');

module.exports = { CatalogRepository, CatalogService, CATEGORIES };

# ADR-034 — Граница состояния и event sourcing

Статус: Accepted

`EventRecord` неизменяем. Просмотр, подтверждение и обработка изменяют отдельные сущности. Доменные модели остаются source of truth текущего состояния; платформа не переходит на event sourcing.
